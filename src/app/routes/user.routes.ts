import { Routes } from '@angular/router';
import { UserComponent } from '../users/user.component';

export const userRoutes: Routes = [
  { path: 'profile', component: UserComponent }, // Use component directly
 ]
